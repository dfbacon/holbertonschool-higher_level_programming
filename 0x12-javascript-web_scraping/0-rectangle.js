#!/usr/bin/node
/*
Write an empty class Rectangle that defines a rectangle.
* 0-main.js:
    #!/usr/bin/node
    const Rectangle = require('./0-rectangle').Rectangle;

    const r1 = new Rectangle();
    console.log(r1);
    console.log(r1.constructor);

Requirements:
* Must use the `function` notation for defining a class.

*/

module.exports = {
  Rectangle: function () {}
};
